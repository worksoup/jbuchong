name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Rust project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install latest nightly
        uses: actions-rs/toolchain@v1
        with:
            toolchain: nightly
            override: true
            components: rustfmt, clippy
      - name: Run cargo check
        uses: actions-rs/cargo@v1
        with:
          command: check  --manifest-path ./rust/Cargo.toml
      - name: Run cargo clippy
        uses: actions-rs/cargo@v1
        with:
          command: clippy  --manifest-path ./rust/Cargo.toml
      - name: Run cargo build
        uses: actions-rs/cargo@v1
        with:
          command: build  --release  --manifest-path ./rust/Cargo.toml
      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v3
        with:
          name: mirai_j4rs_jvm_side
          path: '${{ github.workspace }}/rust/target/release/*.so'
